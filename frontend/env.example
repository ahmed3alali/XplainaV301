# Copy this file to .env.local and adjust values:
#   cp env.example .env.local

# ── Backend (required) ───────────────────────────────────────────────────────
NEXT_PUBLIC_API_BASE=http://localhost:8000

# ── Supabase (required for signup / real users) ──────────────────────────────
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# ── NextAuth (required) ──────────────────────────────────────────────────────
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=change-me-to-a-long-random-string

# ── Demo accounts (create with: cd backend && python seed_demo_users.py) ─────
# Admin panel  → /admin/login
#   Email:    admin@claripath.dev
#   Password: Claripath@Admin1
#
# Student app → /login
#   Email:    student@claripath.dev
#   Password: Claripath@Student1
#
# Dataset user (IBM-style, no signup):
#   ID:       2
#   Password: test000
